{
    "configurations": [
        {
            "name": "Attach to Container & Debug",
            "type": "python",
            "request": "attach",
            "port": 5678,
            "host": "localhost",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}/",
                    "remoteRoot": "/usr/src/app"
                }
            ],
            "preLaunchTask": "Compose Up",
            "postDebugTask": "Compose Down"
        }
    ]
}